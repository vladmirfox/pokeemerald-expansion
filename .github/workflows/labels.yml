name: Labels

on:
  pull_request:
    types: [labeled, unlabeled]

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - name: check labels
        uses: mheap/github-action-required-labels@v5
        with:
          mode: minimum
          count: 1
          labels: |
            General
